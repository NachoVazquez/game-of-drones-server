# Game of Drones server side app

## Steps to run

- Run for start the project `dotnet run` with your powershell targeting **GameOfDrones** project folder. This command will run migrations and create the database if necessary
